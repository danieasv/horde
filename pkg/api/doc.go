// Package api contains the external API for Horde
package api

// The API is implemented as a gRPC service which again is proxied through a
// HTTP interface but it might as well work as a standalone API endpoint.
